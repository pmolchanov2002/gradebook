
    <table class="table">
      <thead>
        <tr>
          <th>Class</th>
          <th>Subject</th>
          <th>Lesson</th>
          <th>Start</th>
          <th>End</th>
          {# Not all students have English name, turning off printing students
          <th>Students</th> 
          #}
        </tr>
      </thead>
      <tbody>
        {%for lesson in lessons %}
          <tr>
            <td>{{ lesson.classOfStudents.englishName }}</td>
            <td>{{ lesson.course.englishName }}</td>
            <td>{{ lesson.period.englishName }}</td>
            <td>{{ lesson.period.startTime|date("H:i") }}</td>
            <td>{{ lesson.period.endTime|date("H:i") }}</td>
            {# <td>
              <table>
                <thead>
                  <th>Student</th>
                  <th>Parent</th>
                  <th>Email</th>
                  <th>Phone</th>
                  <th>Phone</th>
                </thead>
                {% for student in lesson.classOfStudents.students %}
                {% for parent in student.parents %}
                  <tr>
                    <td>{{ student.firstName }} {{ student.firstName }}</td>
                    <td>{{ parent.firstName }} {{ parent.lastName }}</td>
                    <td>{{ parent.email }}</td>
                    <td>{{ parent.mobilePhone }}</td>
                    <td>{{ parent.homePhone }}</td>
                  </tr>
                {% endfor %}
                {% endfor %}
                </table>
            </td> #}
          </tr>
        {% endfor %}
      </tbody>
    </table>
